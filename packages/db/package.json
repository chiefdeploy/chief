{
  "name": "@chief/db",
  "main": "./index.ts",
  "types": "./index.ts",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@prisma/client": "5.16.1"
  },
  "scripts": {
    "dev": "pnpm run db:generate",
    "build": "pnpm run db:generate",
    "db:generate": "prisma generate --no-hints",
    "db:push": "prisma db push --skip-generate"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "prisma": "5.16.1",
    "tslib": "^2.6.3"
  }
}